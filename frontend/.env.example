# Copy to .env.local
# In development the frontend uses /api/proxy to talk to the backend (same origin).
# Set this to your backend API base URL (used by the proxy and server-side).

NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:4000/api
